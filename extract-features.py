# -*- coding: utf-8 -*-

import numpy as np

def extract_features(window):

    # Store total features

    x = []

    # Append each sub-feature to the total vector

    #x = np.append(x, _compute_sub_features(window))

    return x
